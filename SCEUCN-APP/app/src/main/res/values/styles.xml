<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Base application theme. -->
    <style name="ThemeA" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorA_Primary</item>
        <item name="colorPrimaryDark">@color/colorA_PrimaryDark</item>
        <item name="colorAccent">@color/colorA_Accent</item>
    </style>

    <style name="SuccessButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:backgroundTint">@color/colorSuccess</item>
        <item name="android:textColor">#FFF</item>
    </style>

    <style name="CancelButton" parent="Widget.AppCompat.Button.Colored">
        <item name="android:backgroundTint">@color/colorCancel</item>
        <item name="android:textColor">#FFF</item>
    </style>

</resources>
